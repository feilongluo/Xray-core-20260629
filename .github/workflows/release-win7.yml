name: Disabled Upstream Windows 7 Release

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Upstream Windows 7 release is disabled in this fork. SF custom Xray does not publish Windows binaries."
