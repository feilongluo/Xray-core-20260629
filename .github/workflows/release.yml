name: Disabled Upstream Full-Platform Release

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Upstream full-platform release is disabled in this fork. Use Build and Release SF Custom Xray instead."
