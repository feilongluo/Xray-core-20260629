name: Disabled Upstream Scheduled Assets Update

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Upstream scheduled asset update is disabled in this fork. SF custom release packages only the assets required by proxy-ui."
