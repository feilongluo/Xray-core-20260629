name: Disabled Upstream Full Test Matrix

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Upstream full matrix tests are disabled in this fork. SF custom release runs focused WireGuard and outbound manager tests."
