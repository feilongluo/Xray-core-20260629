name: Disabled Upstream Docker Image Release

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Upstream Docker image release is disabled in this fork. proxy-ui owns its runtime image build."
